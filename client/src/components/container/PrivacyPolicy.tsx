import type React from 'react';
import { Container, Box } from '@mui/material';

const PrivacyPolicy: React.FC = () => {
  return (
    <Container maxWidth="md">
      <Box sx={{ my: 4 }}>プライバシーポリシー</Box>
    </Container>
  );
};

export default PrivacyPolicy;
