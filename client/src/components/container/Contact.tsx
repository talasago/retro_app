import type React from 'react';
import { Container, Box } from '@mui/material';

const ServiceTerm: React.FC = () => {
  return (
    <Container maxWidth="md">
      <Box sx={{ my: 4 }}>リヨウキヤク</Box>
    </Container>
  );
};

export default ServiceTerm;
